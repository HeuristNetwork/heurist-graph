{
  "name": "heurist-graph",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone and embeddable graph presentation application for Heurist",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "deploy:heurist": "npm run build && node scripts/deploy-heurist-graph.mjs"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "@heurist/client-core": "file:../heurist-client-core",
    "vis-network": "^10.1.2"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
